@model IEnumerable<EduBrain.Models.ApplicantStudent>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EnrollmentDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ApplicantPersonId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GradeEntering)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EnrollmentDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ApplicantPersonId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GradeEntering)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ApplicantStudentId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ApplicantStudentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ApplicantStudentId })
        </td>
    </tr>
}

</table>
